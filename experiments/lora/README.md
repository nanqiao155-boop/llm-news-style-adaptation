# Source map

Formal runtime configs: `configs/reproduction/`. Aggregates: `results/lora/`. Historical guarded launcher: `scripts/lora_v2.py`.

Historical entry points need the excluded frozen inputs; public quickstart uses synthetic fixtures.
