# Source map

Diagnostic and recovery source: `scripts/lora_v2_production_decode.py` and `scripts/diagnostics/auto_lora_recovery.py`. Cohort-labelled aggregate: `results/stability/summary.json`.

Historical entry points need the excluded frozen inputs; public quickstart uses synthetic fixtures.
