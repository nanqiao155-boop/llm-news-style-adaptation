# Source map

Writer, Reviewer, Reviser and Judge: `demo/agents.py`; conditional flow: `demo/workflow.py`; blind batch evaluation: `scripts/diagnostics/closed_loop_editorial_eval.py`.

Historical entry points need the excluded frozen inputs; public quickstart uses synthetic fixtures.
